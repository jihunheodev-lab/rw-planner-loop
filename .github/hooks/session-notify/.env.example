# Copilot Chat Notification Hook — Environment Variables
# Copy this file to .env and edit values as needed:
#   cp .env.example .env
#
# Shell environment variables always take priority over .env values.

# Notification channels: os, telegram, telegram,os
COPILOT_NOTIFY_CHANNELS=os

# Which events trigger notifications: all, stop, pretooluse, stop,pretooluse, none
COPILOT_NOTIFY_EVENTS=all

# Debug mode: set to 1 to log all tool_name values to stderr
COPILOT_NOTIFY_DEBUG=0

# Notification detail level: short, normal, verbose
#   short   — status only (e.g. "Copilot: 작업 완료!")
#   normal  — + project name, first user message (default)
#   verbose — + session ID, last user message
COPILOT_NOTIFY_DETAIL=normal

# Telegram integration (required when using telegram channel)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
